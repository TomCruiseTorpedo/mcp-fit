# dependencies — bun.lock is the committed lockfile (bun is the dev tool); the npm lockfile is not used
node_modules/
/package-lock.json

# build output
dist/
*.tsbuildinfo

# tool output — mcp-fit writes these to --out (default: cwd); sample-artifacts/ stays committed
/compat.json
/evals.jsonl
/fixes.json
/out/

# env / secrets — never commit (history goes public at the reveal)
.env
.env.*
!.env.example

# logs / coverage / os
*.log
coverage/
.DS_Store

# agent / fleet runtime + internal SSOT — NEVER commit (hygiene gate)
.pi/
*.tmp
.runtime/
.beads/
CLAUDE.local.md
.cline/
