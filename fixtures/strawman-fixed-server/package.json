{
  "name": "mcp-fit-strawman-fixed-server",
  "version": "0.1.0",
  "description": "The strawman server with clean, agent-friendly contracts — paired with strawman-server to demonstrate a keyless red→green deterministic lint delta.",
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
